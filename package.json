{
  "name": "qa-workflow",
  "version": "0.2.0",
  "description": "Guard layer over Playwright Agents \u2014 blocks self-certifying test agents",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "pretest": "npm run build",
    "test": "node --test dist/tests/*.test.js",
    "report": "node dist/tools/report.js",
    "heal-guard": "node dist/tools/heal-guard.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.10.0"
  }
}
